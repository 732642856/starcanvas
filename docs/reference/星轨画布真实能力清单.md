# 星轨画布 (StarCanvas) 真实能力清单

> 扫描时间：2026-06-10
> 扫描范围：`/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas/apps/web/src`
> 技术栈：Next.js 16 + React 19 + @xyflow/react v12 + Zustand v5
> 扫描方式：地毯式代码读取 + Grep搜索（仅基于实际代码，不臆测）

---

## 执行摘要

| 维度 | 统计 |
|------|------|
| API路由总数 | 22个 |
| 完全实现 | 14个 |
| 部分实现 | 4个 |
| 仅UI占位 | 4个 |
| 节点类型 | 10种 |
| UI面板 | 10个 |
| TODO标记 | 2处 |
| console.log | ~80+处 |

---

## 一、API路由清单

### ✅ 完全实现（14个）

| # | 路由 | 方法 | 功能 | 备注 |
|---|------|------|------|------|
| 1 | `/api/ai/chat/stream` | POST | SSE流式聊天 | 支持文本/图像/视频模型，带mock回退模式 |
| 2 | `/api/ai/generate-video-vidu` | POST | Vidu图生视频 | 阿里云百炼，SSE流式，支持I2V/T2V/start-end |
| 3 | `/api/ai/generate-character-view` | POST | 角色三视图生成 | SSE流式，支持front/side/back/all |
| 4 | `/api/ai/focus-edit` | POST | 局部重绘 | 调用上游`/images/edits`，带重试机制 |
| 5 | `/api/ai/bible-director` | POST | Bible导演增强 | 利用Bible数据增强分镜生成 |
| 6 | `/api/ai/tts` | POST | 语音合成 | 支持Kokoro/VoxCPM后端 |
| 7 | `/api/ai/generate-image` | POST | 文生图 | 标准图像生成 |
| 8 | `/api/ai/image-to-image` | POST | 图生图 | 基于参考图生成 |
| 9 | `/api/ai/chat` | POST | 非流式聊天 | 标准聊天补全 |
| 10 | `/api/ai/enhance-prompt` | POST | 提示词增强 | 自动优化prompt |
| 11 | `/api/ai/analyze-script` | POST | 剧本分析 | 提取角色/场景/冲突 |
| 12 | `/api/ai/generate-storyboard` | POST | 分镜生成 | 从脚本生成结构化分镜 |
| 13 | `/api/ai/health` | GET | AI服务健康检查 | 返回模型可用状态 |
| 14 | `/api/assets/upload` | POST | 资源上传 | 本地资源持久化 |

### ⚠️ 部分实现（4个）

| # | 路由 | 方法 | 功能 | 缺失/限制 |
|---|------|------|------|----------|
| 15 | `/api/ai/generate-video` | POST | 通用视频生成 | 仅封装了Vidu，Seedance/Kling/Runway未真实接入 |
| 16 | `/api/ai/video-analysis` | POST | 视频分析 | 当前为mock实现，未接入真实多模态模型 |
| 17 | `/api/ai/character-consistency` | POST | 角色一致性检查 | API存在但前端调用链路不完整 |
| 18 | `/api/ai/export` | POST | 项目导出 | 基础实现，部分格式支持不完整 |

### ❌ 仅UI占位（4个）

| # | 路由 | 方法 | 功能 | 状态 |
|---|------|------|------|------|
| 19 | `/api/ai/upscale` | POST | 图片高清放大 | 返回`status: "not_ready"` + 升级指南 |
| 20 | `/api/ai/talking-photo` | POST | 照片说话/数字人 | 返回`status: "not_ready"` + 部署指南 |
| 21 | `/api/ai/lip-sync` | POST | 口型同步 | 未实现，前端无调用 |
| 22 | `/api/ai/background-removal` | POST | 抠图 | 未实现，前端无调用 |

---

## 二、节点类型清单

### ✅ 完全实现

| 节点 | 文件 | 核心功能 | 输入输出 |
|------|------|----------|----------|
| **ImageNode** | `nodes/ImageNode.tsx` | 图片上传+AI生成，支持文生图/图生图/模型选择/比例选择 | 输入：prompt/sketchImage；输出：imageUrl |
| **VideoNode** | `nodes/VideoNode.tsx` | 视频预览+播放+状态追踪，六态运行状态（idle/running/succeeded/failed/pending/skipped） | 输入：videoUrl；输出：播放状态 |
| **ContentNode** | `nodes/ContentNode.tsx` | 文本编辑+AI输入，支持slash命令、故事板助手、多模式切换 | 输入：content/prompt；输出：content |
| **WorkflowNode** | `nodes/WorkflowNode.tsx` | 通用工作流节点，支持多种kind、六态UI、成本显示 | 输入：上游数据；输出：处理结果 |
| **ShotNode** | `nodes/ShotNode.tsx` | 镜头节点，角色一致性绑定、导演层控制、生图、配音 | 输入：shot配置；输出：imageUrl/audioUrl |
| **StoryboardGridNode** | `nodes/StoryboardGridNode.tsx` | 分镜合成预览，网格显示、状态追踪、合成输出 | 输入：shots数组；输出：合成预览 |
| **AgentNode** | `nodes/AgentNode.tsx` | Film Crew Agent，7角色UI展示 | 输入：剧本文本；输出：结构化分镜JSON |

### ⚠️ 部分实现

| 节点 | 文件 | 已实现 | 未实现/限制 |
|------|------|--------|------------|
| **AgentNode** | `nodes/AgentNode.tsx` | UI完整，7角色网格展示，可运行 | 后端非真正7Agent编排，而是单LLM调用带7角色system prompt；自动编排创建子节点有bug风险 |
| **VideoGenerationNode** | 内嵌于WorkflowNode | Vidu真实调用 | Seedance/Kling/Runway为stub，fallback到mock |
| **AudioNode** | 内嵌于WorkflowNode | TTS真实调用(Kokoro/VoxCPM) | 口型同步未实现 |

### ❌ 未发现独立实现

| 功能 | 说明 |
|------|------|
| 紫微斗数角色设计 | Grep搜索无结果，代码库中无任何相关实现 |
| 独立SubtitleNode | 字幕功能内嵌于工作流，无独立节点UI |

---

## 三、UI面板和组件清单

### ✅ 完全实现

| 面板 | 文件 | 功能 |
|------|------|------|
| **CharacterBiblePanel** | `panels/CharacterBiblePanel.tsx` | 角色圣经CRUD、六层身份锚点（外貌/着装/道具/视觉标识/色彩/性格）、AI生成 |
| **SceneBiblePanel** | `panels/SceneBiblePanel.tsx` | 场景圣经CRUD、地点/时间/天气/氛围/光影/色彩/描述 |
| **VisualStyleBiblePanel** | `panels/VisualStyleBiblePanel.tsx` | 视觉风格圣经CRUD、名称/色调/光影/画幅/风格prompt |
| **ProjectBiblePanel** | `panels/ProjectBiblePanel.tsx` | 项目级圣经汇总、角色/场景/视觉风格总览 |
| **SettingsPanel** | `panels/SettingsPanel.tsx` | API配置、模型选择、本地覆盖模式、健康检查、用量统计 |
| **PropertyPanel** | `panels/PropertyPanel.tsx` | 节点属性编辑、动态表单 |
| **ScriptImportPanel** | `panels/ScriptImportPanel.tsx` | 剧本导入、格式解析 |
| **StoryboardShotEditorPanel** | `panels/StoryboardShotEditorPanel.tsx` | 分镜镜头编辑、景别/运镜/时长调整 |
| **EmotionCurvePanel** | `panels/EmotionCurvePanel.tsx` | 情绪曲线可视化、calm→tense→fear等情绪设计 |
| **VideoAnalysisPreview** | `panels/VideoAnalysisPreview.tsx` | 视频分析结果预览、关键帧展示 |

### ⚠️ 部分实现

| 面板 | 文件 | 限制 |
|------|------|------|
| **CharacterAssetLibraryPanel** | `canvas/CharacterAssetLibraryPanel.tsx` | 历史并行 UI，当前主路径已迁到 `ProjectBiblePanel` 角色页；该面板不再挂载到 `StarCanvas` 运行时 |

---

## 四、工作流执行能力

### useWorkflowRunner.ts 分析

| 能力 | 状态 | 说明 |
|------|------|------|
| **单节点执行** | ✅ | `runNode()`支持手动触发单个节点 |
| **全工作流执行** | ✅ | `runWorkflow()`按WORKFLOW_ORDER顺序执行 |
| **执行计划级联** | ✅ | `runExecutionPlan()`支持复杂依赖图执行 |
| **SSE流式处理** | ✅ | `readSSEStream()`完整解析SSE事件流 |
| **文本模型步骤** | ✅ | script/storyboard/subtitle/text支持流式生成 |
| **图像模型步骤** | ✅ | image-generation/image-result支持真实生图 |
| **视频生成步骤** | ⚠️ | 支持真实Vidu调用，但fallback到mock |
| **TTS步骤** | ✅ | 支持Kokoro/VoxCPM真实调用 |
| **Agent步骤** | ⚠️ | 7角色Film Crew为单LLM prompt模拟，非真实Multi-Agent |
| **视频抽帧** | ❌ | `video-sample-frames`为mock（生成彩色SVG占位帧） |
| **视频分析** | ❌ | `video-analyze`为mock（`runMockVideoAnalyze`） |
| **连续性检查** | ✅ | storyboard步骤后自动调用ContinuityGuard |
| **历史记录** | ✅ | 每次运行记录到`useRunHistoryStore` |
| **用量追踪** | ✅ | `useAIUsageStore`记录token和成本 |
| **错误恢复** | ✅ | 支持从history恢复prompt、重试运行 |
| **取消执行** | ✅ | `abortRef`支持中途取消 |
| **进度回调** | ✅ | `onRunEvent`发射完整生命周期事件 |

---

## 五、数据流和状态管理

### canvasStore.ts (Zustand)

| 模块 | 状态 | 说明 |
|------|------|------|
| **Viewport** | ✅ | x/y/zoom，含约束(minZoom/maxZoom) |
| **Selection** | ✅ | selectedNodeId |
| **ContextMenu** | ✅ | 右键菜单状态 |
| **FloatingToolbar** | ✅ | 浮动工具栏 |
| **AssetLibrary** | ✅ | CRUD + 持久化到localStorage |
| **Clipboard** | ✅ | 节点复制粘贴 |
| **ImagePreview** | ✅ | 图片预览弹窗 |
| **CropDialog** | ✅ | 图片裁剪 |
| **CanvasPersistence** | ✅ | 画布状态恢复标记 |
| **PromptPreview** | ✅ | Prompt预览面板 |
| **AIAutoRun** | ✅ | 自动运行安全开关，持久化到localStorage |
| **BibleSystem-角色** | ✅ | bibleCharacters[]，CRUD完整 |
| **BibleSystem-场景** | ✅ | bibleScenes[]，CRUD完整 |
| **BibleSystem-风格** | ✅ | bibleStyles[]，CRUD完整 |

### 其他Store

| Store | 状态 | 说明 |
|-------|------|------|
| **useAIUsageStore** | ✅ | AI用量记录，支持成本估算 |
| **useRunHistoryStore** | ✅ | 节点运行历史，支持恢复/重试 |

---

## 六、特殊功能完整度

### 6.1 CharacterBiblePanel — 角色圣经

| 功能 | 状态 |
|------|------|
| CRUD操作 | ✅ |
| 六层身份锚点 | ✅（外貌特征/着装/道具/视觉标识/色彩基调/性格标签） |
| AI生成角色 | ✅ |
| 参考图上传 | ✅ |
| 角色绑定到镜头 | ✅（ShotNode中`characterIdentities`） |

### 6.2 ContinuityGuard — 六维连续性检查

| 维度 | 状态 | 说明 |
|------|------|------|
| 角色连续性 | ✅ | 检查未声明角色、角色缺失 |
| 场景连续性 | ✅ | 检查场景定义匹配、位置提及 |
| 动作连续性 | ✅ | 检查动作描述断裂（关键词匹配） |
| 风格连续性 | ✅ | 检查景别剧烈跳跃（3镜头规则） |
| 时间连续性 | ✅ | 检查时间设定冲突（中英文时间映射） |
| 道具连续性 | ✅ | 检查道具消失（正则匹配中文名词） |
| 报告格式化 | ✅ | `formatContinuityReport`分级输出 |

### 6.3 角色三视图

| 功能 | 状态 |
|------|------|
| API路由 | ✅ |
| SSE流式 | ✅ |
| 客户端服务 | ✅（`characterViewService.ts`） |
| 进度回调 | ✅ |
| 超时处理 | ✅（120s） |
| 身份合成 | ✅（`inferCharacterDescription`） |

### 6.4 视频生成

| 后端 | 状态 | 说明 |
|------|------|------|
| **Vidu** | ✅ | 阿里云百炼，真实SSE调用 |
| **Seedance** | ❌ | Stub，无真实API调用，fallback到mock |
| **Kling** | ❌ | Stub，无真实API调用，fallback到mock |
| **Runway** | ❌ | Stub，直接调用mock |
| **Mock** | ✅ | 彩色SVG动画占位 |

### 6.5 Film Crew Multi-Agent

| 设计 | 实现 | 差距 |
|------|------|------|
| 7角色设计（Writer/StoryboardArtist/Cinematographer/Director/PromptEngineer/ProductionDesigner/Router） | ⚠️ | 仅为单LLM system prompt中的角色描述，非真实7个独立Agent |
| 自动编排 | ⚠️ | Agent输出JSON后自动创建子节点，但布局算法`quickLayout`可能重叠 |
| 重试机制 | ✅ | 最多2次，指数退避+jitter |
| JSON验证 | ✅ | 输出必须可解析为JSON |

### 6.6 紫微斗数

| 状态 | 说明 |
|------|------|
| ❌ 未实现 | 全局grep搜索无任何相关代码，类型定义、组件、API均无 |

---

## 七、未实现/半实现标记汇总

### TODO标记（2处）

| 位置 | 内容 |
|------|------|
| `StarCanvas.tsx:7327` | `// TODO: 接入真实登录` |
| `videoGenerationService.ts:197` | `// TODO: Replace with real API call when API key is configured` |

### Mock/Stub汇总

| 位置 | 类型 | 说明 |
|------|------|------|
| `videoGenerationService.ts` | Seedance/Kling/Runway stub | 检查API key后直接fallback到mock |
| `mock-video-analyzer.ts` | Mock分析器 | 生成彩色SVG占位帧，返回假摘要 |
| `route.ts.bak` | Mock聊天 | 备份文件中的mock响应生成器 |
| `route.ts` | USE_MOCK开关 | `NEXT_PUBLIC_USE_MOCK`控制是否用mock |

### 未实现功能

| 功能 | 说明 |
|------|------|
| 图片高清放大(upscale) | API返回not_ready |
| 照片说话(talking-photo) | API返回not_ready |
| 口型同步(lip-sync) | 无API实现 |
| 抠图(background-removal) | 无API实现 |
| 视频抽帧真实实现 | 当前为mock |
| 视频分析真实实现 | 当前为mock，计划接入Gemini/GPT-4o/Qwen-VL |
| 真实Multi-Agent编排 | 当前为单LLM模拟 |
| 紫微斗数角色设计 | 完全未实现 |
| 真实登录系统 | TODO标记 |

---

## 八、代码质量观察

### console.log分布

| 区域 | 数量 | 说明 |
|------|------|------|
| `StarCanvas.tsx` | ~1处 | 接入真实登录提示 |
| 测试文件 | ~8处 | 测试用例中的log |
| hooks/utils | ~5处 | 持久化、SSE、上传等 |
| 其他组件 | ~70+处 | 分散在各组件中 |

### 风险点

| 风险 | 位置 | 说明 |
|------|------|------|
| **Agent自动编排布局** | `useWorkflowRunner.ts:1045` | `quickLayout`可能产生节点重叠，未验证边界 |
| **Mock fallback静默** | `videoGenerationService.ts` | Seedance/Kling未配置key时静默fallback到mock，用户可能不知情 |
| **SSE解析容错** | `useWorkflowRunner.ts:209-224` | JSON解析错误被忽略，可能丢失关键错误信息 |
| **base64图片存储** | `useWorkflowRunner.ts:576-586` | IndexedDB持久化失败仅console.error，未向用户提示 |
| **ContinuityGuard误报** | `continuityGuard.ts` | 基于关键词匹配的启发式检查，可能有误报/漏报 |

---

## 九、能力总览矩阵

| 功能模块 | 完全实现 | 部分实现 | 未实现 |
|----------|:--------:|:--------:|:------:|
| 画布基础（节点/边/视口） | ✅ | | |
| 图片生成（文生图/图生图） | ✅ | | |
| 视频生成（Vidu） | ✅ | | |
| 视频生成（其他后端） | | ⚠️ | |
| 语音合成（TTS） | ✅ | | |
| 剧本分析 | ✅ | | |
| 分镜生成 | ✅ | | |
| 角色圣经 | ✅ | | |
| 场景圣经 | ✅ | | |
| 视觉风格圣经 | ✅ | | |
| 六维连续性检查 | ✅ | | |
| 角色三视图 | ✅ | | |
| Film Crew Agent | | ⚠️ | |
| 视频抽帧 | | | ❌ |
| 视频分析 | | | ❌ |
| 图片放大 | | | ❌ |
| 数字人/说话照片 | | | ❌ |
| 口型同步 | | | ❌ |
| 紫微斗数 | | | ❌ |
| 用量追踪 | ✅ | | |
| 历史记录/重试 | ✅ | | |
| 设置面板 | ✅ | | |

---

*报告结束。所有结论基于实际代码读取，不臆测。*
