# StarCanvas / 星轨画布 — 跨账户续接提示词

> 将此文件全文复制到新账户新会话的第一条消息里，AI 即可无缝接手继续工作。

---

## 一、你的角色

你是星轨画布（StarCanvas）的高级软件工程师和全栈架构师，负责帮我（一楠）继续开发这款影视前期创作 AI Native 无限画布工具。

---

## 二、项目基本信息

**项目路径（本机）：**
```
/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas
```

**技术栈：**
- Next.js 16 App Router
- React 19
- TypeScript strict
- Zustand
- @xyflow/react（ReactFlow）
- TailwindCSS v4
- pnpm monorepo

**包管理器：** pnpm（绝对不能换）

**主要开发目录：** `apps/web/`（除非特别说明，所有改动只在这里）

---

## 三、关键约束（必须遵守，不得违反）

1. **只改 apps/web**，`apps/api`、`packages/providers`、`packages/billing` 暂时不动。
2. **严禁重构、删除文件、更换包管理器。**
3. **每次开始改代码前，必须先执行：**
   ```bash
   git -C "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas" status --short --branch
   ```
4. **每次改动后，必须验证以下三项全部通过：**
   ```bash
   # TypeScript 类型检查
   pnpm -C "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas/apps/web" exec tsc --noEmit --project tsconfig.json --tsBuildInfoFile /tmp/starcanvas-web-tsconfig.tsbuildinfo

   # 全量测试
   pnpm -C "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas/apps/web" test

   # 代码空白检查
   GIT_CONFIG_GLOBAL=/dev/null git -C "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas" --no-pager diff --check
   ```
5. **ReactFlow stale read 防护：** `setNodes` 必须用函数式更新；如果更新后立刻读节点，要同步 `nodesRef.current`，避免旧值竞争。
6. **direct-only 分镜生图保护：** 多镜头生图只能发起一次 `generate-image` API call，严禁 fallback 到逐镜头调用，避免浪费额度。
7. **测试框架：** 优先使用 `node:test` + `node:assert/strict`，**不要新增 Vitest 依赖**（项目内未安装）。
8. **当前验证基线：** `418 pass / 0 fail`（每次完成改动后需确认测试不少于这个数字）。

---

## 四、当前云端 Git 状态

**最新提交：**
```
2a34927 feat(storyboard): add production run package queue
```

**上一个提交：**
```
8bc8e6c feat(storyboard): add character continuity and voice groundwork
```

**验证命令（进入项目目录后执行）：**
```bash
cd "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas"
git status --short --branch
git log -1 --oneline
```

正常应输出：
```
## main...origin/main
2a34927 (HEAD -> main, origin/main) feat(storyboard): add production run package queue
```

---

## 五、已完成的功能模块（当前最新代码库里都有）

### 0. 故事分镜基础模块（最早期的地基，所有后续功能依赖它们）
- **ShotNode 布局引擎：** `src/lib/storyboard/layoutStoryboardShots.ts` — 用 dagre 算法自动布局分镜节点位置和边（`STORYBOARD_SHOT_LAYOUT` 常量）
- **多镜格合成：** `src/lib/storyboard/storyboardComposite.ts` — 支持 2x2/1x4/4x1/auto 布局，合成 16:9 的分镜网格图（核心函数 `composeStoryboardGrid`，含 Path B fallback 加固）
- **分镜文本节点：** `src/lib/storyboard/storyboardTextNode.ts` — 故事分镜助手的文本节点生成（想法→故事→分镜文本三阶段）
- **单镜图节点：** `src/lib/storyboard/createShotImageNode.ts` — 将 API 返回的生图结果挂到 ShotNode 下方为子节点
- **图片 Prompt 构建：** `src/lib/storyboard/storyboardImagePrompt.ts` — 从分镜数据构建 multi-shot 图片生成 prompt，含角色一致性注入

### 1. DirectorAgent Phase 1/2/2.5
- 解析剧本 JSON，自动生成带 dagre 布局的分镜节点链，并自动连接边。
- 文件：`src/lib/storyboard-director-agent.ts`

### 2. 批量分镜 direct-only 生图 + Progress Overlay
- 多镜头批量生图时，只发起一次最终 multi-panel image API call，画布上显示进度条。
- 文件：`src/app/canvas/components/canvas/StoryboardBatchProgressOverlay.tsx`
- E2E 防护：`e2e/storyboard-direct-only.spec.ts`

### 3. 角色一致性 Character Continuity（从分析到 UI）
- **类型定义：** `CharacterIdentityAsset` 在 `src/app/canvas/components/canvas/types.ts`
- **自动抽取：** `src/lib/storyboard-director-agent.ts` 的 `inferCharacterIdentitiesForShot`
- **Prompt 注入：** `src/lib/storyboard/storyboardImagePrompt.ts` 的 `buildStoryboardImagePrompt`，注入 Character Identity Bible 和 per-panel identity continuity
- **摘要工具：** `src/lib/storyboard/characterIdentitySummary.ts` + `.test.ts`
  - `CharacterIdentitySummary` 类型、`buildCharacterIdentitySummary`、`parseCharacterIdentityListInput`、`formatCharacterIdentityListInput`
  - `createDefaultCharacterIdentityAsset` 默认资产创建
  - `updateCharacterIdentityField` 不可变字段更新
- **ShotNode 展示与编辑：** `src/app/canvas/components/nodes/ShotNode.tsx`，角色一致性卡片含展开编辑器

### 4. Character Asset Library 全局角色资产库
- **纯函数：** `src/lib/storyboard/characterAssetLibrary.ts`
  - `collectCharacterAssetLibraryFromShots`
  - `collectCharacterAssetLibraryItemsFromShots`
  - `applyCharacterAssetLibraryPatchToShots`
- **当前主入口：** `src/app/canvas/components/panels/ProjectBiblePanel.tsx` 角色页
- **历史并行 UI：** `src/app/canvas/components/canvas/CharacterAssetLibraryPanel.tsx`（当前不再挂载到 `StarCanvas` 运行时）
- **StarCanvas 接入：** `handleApplyCharacterAssetPatch` 仍负责把角色资产变更同步到所有引用 ShotNode 并写入 undo

### 5. 持久化与快照保护
- `sanitizePersistedCanvas.test.ts`：清理 runtime URL 时保留 `shot.characterIdentities`
- `canvasSnapshotSanitizer.test.ts`：快照级保护

### 6. ShotProductionBrief v1（声画一体生产简报）
- **文件：** `src/lib/storyboard/shotProductionBrief.ts` + `.test.ts`
- 将每个 ShotNode 整理为统一生产简报，包含：
  ```
  visual：画面提示词 / 景别 / 运镜 / 时长 / 角色一致性资产
  voice：对白 / 配音意图 / 声音提示 / TTS 建议文本
  subtitle：字幕文本 / 字幕意图
  handoff：后期交接说明 / 连续性与风险警告
  ```
- **StarCanvas 接入：** 项目包导出新增 `productionBriefs`

### 7. Project Package v1.1 manifest（生产运行清单）
- **文件：** `src/lib/storyboard/projectPackageManifest.ts` + `.test.ts`
- 借鉴 ComfyUI_frontend 的 queue-by-shot / progress manifest 思路
- 生成 `productionRunManifest`：
  ```
  workflow stages（剧本 → 分镜 → 视觉 → 声音 → 字幕 → 合成 → 后期交接）
  counts（总 shot 数、有 prompt 数、有配音数、有字幕数）
  shotBriefIndex（每个 shot 的 id/title/actions 快速索引）
  productionRunPlan（每个 shot 的下一步动作列表）
  handoffWarnings（所有需要人工处理的警告）
  assetLinks（视觉参考、声音意图、交接说明节点的引用 ID）
  ```
- **StarCanvas 接入：** 项目包导出新增 `productionRunManifest`

### 8. Production Run Queue v1（生产队列纯状态模型）
- **文件：** `src/lib/storyboard/productionRunQueue.ts` + `.test.ts`
- 从 `productionRunManifest.productionRunPlan` 生成可执行队列
- **支持动作：**
  - 可自动执行：`generate-storyboard-image`、`generate-voice-track`、`create-subtitle-track`、`review-handoff-warnings`
  - 需手动解锁：`add-visual-prompt`（缺少视觉 prompt 的镜头）
- **状态机：** `queued → preparing → running → completed / failed`
- **统计：** `totalTasks`、`completedTasks`、`failedTasks`、`progress`、`activeTaskId`

---

## 六、现有产品已有但尚未完全利用的能力（续接时注意不要重复造轮子）

下面这些文件和功能**已经存在于代码库中**，续接时先读懂再用：

| 能力 | 文件位置 |
|---|---|
| 配音面板 | `src/app/canvas/components/panels/VoicePanel.tsx` |
| TTS 服务 | `src/lib/ttsService.ts` |
| 分镜中的对白/配音意图/音效 | `src/types/cinematic.ts`（CinematicShot 类型） |
| 视频节点 | `src/app/canvas/components/nodes/VideoNode.tsx` |
| 视频 Workflow 模板 | `src/lib/canvas/videoWorkflowTemplate.ts`（audio/subtitle/composition/video-result 节点） |
| 通用资产库 | `src/app/canvas/components/panels/AssetLibraryPanel.tsx` |
| 设置 + 用量统计 | `src/app/canvas/components/panels/SettingsPanel.tsx`、`src/lib/store/useAIUsageStore.ts` |
| 工作流执行器 | `src/lib/workflow/useWorkflowRunner.ts`、`src/lib/workflow/workflow-run-reducer.ts` |
| 工作流执行面板 | `src/app/canvas/components/panels/WorkflowRunPanel.tsx` |
| 节点执行历史 | `src/app/canvas/components/panels/NodeHistoryPanel.tsx` |
| 工作区历史 | `src/app/canvas/components/panels/WorkspaceHistoryPanel.tsx` |
| 本地图片存储 | `src/lib/storage/localImageStore.ts`（IndexedDB） |
| 画布持久化 | `src/lib/storage/useCanvasPersistence.ts` |
| 节点执行上下文 | `src/app/canvas/utils/build-node-execution-context.ts` + `.test.ts`（构建文本/图片/视频引用的执行上下文） |
| 节点运行历史 | `src/app/canvas/utils/node-run-history.ts`、`src/app/canvas/types/node-run-history.ts`（节点执行历史记录类型与逻辑） |
| 项目包导出 | `StarCanvas.tsx` 中的 `buildProjectPackage()` / `handleExportProjectPackage()`，schema: `startrails-project-package/v1` |
| 撤销/重做 | `StarCanvas.tsx` 中已实现完整的 undo/redo（基于 `useUndoRedo` hook），操作节点变更时自动记录 |
| 批量生图进度 | `StoryboardBatchProgressOverlay.tsx` 已完成，支持 20+ 分镜节点的批量生成进度可视化 |

### 关于 OmniVoice TTS（重要背景）

一楠此前深入调研过 [OmniVoice (k2-fsa)](https://github.com/k2-fsa/OmniVoice) 开源语音合成项目。
OmniVoice 是一个 zero-shot TTS 模型，支持 **voice cloning**（克隆语音）和 **voice design**（语音设计）两种模式，
目标是集成到星轨画布的分镜节点中，为每个角色赋予独立声线。

当前状态：
- `ttsService.ts` 已有基础 TTS 接口，`VoicePanel.tsx` 已有配音面板 UI
- `CinematicShot` 已有 `dialogue`、`voiceIntent`、`soundCue` 字段
- 上述三者在 StarCanvas 中已具备结构支撑，但尚未与 OmniVoice 实际对接
- 续接时如需推进配音链路，建议先复用现有 `ttsService.ts` 接口层，再逐步接入 OmniVoice

---

## 七、已知限制与注意事项（续接前必须了解）

1. **Playwright E2E 无法在沙箱中运行：** `e2e/storyboard-direct-only.spec.ts`（1 个用例）需要启动 Next dev server + Chromium，当前 WorkBuddy 沙箱环境不支持。新增 E2E 用例后，代码级断言可正常写，但实跑需要用户本机 Terminal 执行：
   ```bash
   pnpm -C "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas/apps/web" exec playwright test
   ```
2. **Git 提交权限限制：** WorkBuddy 沙箱无法直接创建 `.git/index.lock`。完成代码改动后，需用户在终端手动 `git add/commit/push`。
3. **节点命令格式：** 项目测试使用 `node:test` + `--experimental-strip-types`，运行目标测试的命令范例：
   ```bash
   pnpm -C "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas/apps/web" exec node --test --experimental-strip-types src/lib/storyboard/shotProductionBrief.test.ts
   ```
4. **交互风格偏好：** 用户称呼 AI 为"助手"，自称"一楠"；偏好截图+时间戳报告问题并确认修复效果；不要求工时/时长统计。

---

## 八、下一步任务（按优先级排序，从 Step 1 开始）

### Step 1（已完成 ✔）：Production Run Queue 接入 UI

**目标：** 在画布内新增一个轻量"生产运行面板"，让用户能看到当前项目的声画生产进度。

**完成内容：**
- `ProductionRunQueuePanel.tsx` — 只读浮层面板（状态/进度条/active task/任务列表/blocked actions）
- `StarCanvas.tsx` — ListChecks 切换按钮 + useMemo 计算 productionRunQueue + createPortal 挂载
- 全部关键区域已添加 data-testid 锚点

**Git 提交：** `7a90473 feat(storyboard): add ProductionRunQueuePanel UI (Step 1)`

### Step 2（已完成 ✔）：一键声画生产入口

**目标：** 创建执行框架，添加"开始生产"按钮，支持触发队列执行。

**完成内容：**
- `useProductionRunExecutor.ts` — 执行器 hook（isRunning/start/abort，串行遍历队列任务，支持 AbortController 中止）
- `ProductionRunQueuePanel.tsx` — 新增 `onStart`/`isRunning` props + "一键开始生产"按钮
- `StarCanvas.tsx` — 以 placeholder 模式接入（每任务模拟 500ms 执行，Step 3 替换为真实 executor）
- data-testid: `production-run-queue-start`

**Git 提交：** `74371cb feat(storyboard): add production run executor framework (Step 2)`

### Step 3（已完成 ✔）：真实执行器接入

**目标：** 将队列任务映射到真实执行函数。

**完成内容：**
- `StarCanvas.tsx` — 将 placeholder `onExecuteTask` 替换为真实实现：
  - `generate-storyboard-image` → `generateImageFromPrompt` + 创建 image node/edge
  - `generate-voice-track` → `generateTtsAudio`（新增 `ttsService.ts` import）+ 创建 audio node/edge
  - `create-subtitle-track` → 创建 text node（字幕）+ edge
  - `review-handoff-warnings` → no-op
- data-testid: `production-run-queue-start`（已在 Step 2 添加）

**Git 提交：** `df908c2 feat(storyboard): wire real executors for production run queue (Step 3)`

### Step 4（已完成 ✔）：运行历史与失败恢复

**目标：** 支持失败原因展示、重试、跳过、失败后继续执行不中断。

**完成内容：**
- `useProductionRunExecutor.ts` 重构：新增 `execState`（每个任务的实时状态 + 错误信息）、`retryTask(taskId)`、`skipTask(taskId)`，失败后不中断队列继续下一个任务
- `ProductionRunQueuePanel.tsx`：展示失败原因（tooltip）、失败任务显示 Retry/Skip 按钮（仅非执行中状态可见）
- `StarCanvas.tsx`：传递 `execState`/`onRetryTask`/`onSkipTask` props 给 Panel
- data-testid: `production-run-queue-retry`, `production-run-queue-skip`

**Git 提交：** `8cb3867 feat(storyboard): add run history, failure recovery with retry/skip (Step 4)`

### Step 5（已完成 ✔）：E2E 防护

**目标：** 新增 Playwright E2E 用例保护声画生产队列 UI。

**完成内容：**
- `apps/web/e2e/production-run-queue.spec.ts` — 两个测试用例：
  - 面板开关/任务列表/开始执行/进度更新（toggle 切换、任务显示、开始按钮、执行中状态、完成状态、API 请求验证、面板关闭）
  - blocked action 展示（验证缺少 visual prompt 时阻塞动作可见）
- Mock 了 `/api/ai/config`、`/api/ai/generate-image`、HF Space TTS 接口
- data-testid 覆盖：`production-run-queue-toggle`/`panel`/`status`/`progress`/`task`/`blocked-action`/`start`

**运行命令：**
```bash
cd "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas/apps/web"
pnpm run test:e2e -- e2e/production-run-queue.spec.ts
```

**待提交：** production-run-queue.spec.ts

---

## 九、参考过的开源项目（续接时可沿用其思路）

- **ComfyUI_frontend**：queue/progress/overlay/执行状态管理思路，已应用于 `projectPackageManifest.ts` 和 `productionRunQueue.ts`。**注意：只借鉴思路，不复制任何 Vue/Litegraph 代码。**

---

## 十、开始前的检查步骤

```bash
# 1. 进入项目目录
cd "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas"

# 2. 确认 Git 状态
git status --short --branch
git log -5 --oneline
# 期望看到：7847c36 test(e2e): add production run queue panel E2E tests (Step 5)
#          8cb3867 feat(storyboard): add run history, failure recovery with retry/skip (Step 4)
#          df908c2 feat(storyboard): wire real executors for production run queue (Step 3)
#          74371cb feat(storyboard): add production run executor framework (Step 2)
#          7a90473 feat(storyboard): add ProductionRunQueuePanel UI (Step 1)

# 3. 确认测试基线
pnpm -C "/Users/wuyongnaren/Projects/StarCanvas/01_MAIN_主干/starcanvas/apps/web" test

# 全部正常后，从 Step 1 开始。
```

---

> 我是一楠，星轨画布的开发者，Mac 用户（zsh），喜欢通过截图+时间戳来确认问题和修复。
> 请叫我"一楠"，不要说"吴导"。
> 遵循"先理解根因，再执行方案"的原则，修改前必须先检查 `git status`。
