# ARCHIVE — 2026-06-10 历史能力审计快照

> 警告: 本文档是阶段性审计结果，不是当前主仓的唯一能力事实源。
> 当前能力事实请先看: `docs/reference/current-capability-map.md`
> 使用规则: 如本文结论与当前代码、当前测试、`current-capability-map.md` 冲突，一律以后者为准。

# 星轨画布（StarCanvas）真实能力清点与TapNow对标报告

> 日期：2026-06-10
> 检查人：AI审计Agent（地毯式扫描）
> 范围：apps/web/src 全部代码 + API路由 + 节点系统 + 工作流执行
> 结论：**先给结论——星轨画布的核心创作链路（剧本→分镜→生图→生视频→配音）已真实打通，但存在1个P0阻塞bug、16个P1重要问题、18个P2建议项。与TapNow相比，我们在"玄学+编剧"深度融合上有独特优势，但在"专业控制面板"和"社区生态"上差距明显。**

---

## 一、TapNow真实流程复盘（用户视角的16步操作）

基于对TapNow截图和文档的深度分析，一个真实用户从打开到出片的完整流程是：

```
打开黑色网格画布
  ↓
左侧工具栏上传角色图/产品图 → 画布上出现图片节点
  ↓
右侧Greeting面板@引用图片 → AI读取并分析素材
  ↓
新建Text节点写下视频目标（30秒竖屏科幻短片）
  ↓
底部Gemini输入框生成脚本 → 产出ScriptNode
  ↓
AI拆分为6个分镜节点（StoryboardNode）
  ↓
每个分镜连接角色参考图 → 创建生图节点 → 生成分镜图
  ↓
分镜图连接视频节点 → 生成6个视频片段
  ↓
添加旁白Text节点 → 连接语音节点生成音频
  ↓
添加字幕节点 + 背景音乐节点
  ↓
全部接入合成节点 → 生成最终视频
  ↓
哪里不满意，回到对应节点局部重跑
```

**TapNow的核心设计哲学**：
1. **节点即流程** —— 不是装饰，每个节点代表一个真实生产步骤
2. **Chat即指挥** —— AI读取画布、理解节点关系、生成新节点、驱动执行
3. **局部重跑** —— 修改一个分镜，只需重跑该分镜对应的生图+视频节点
4. **专业控制** —— Cinema Lab提供多角度拖拽调姿态、火柴人动作参考、魔法棒焦点编辑
5. **社区即教程** —— TapTV社区，12万+可克隆工作流

---

## 二、星轨画布真实能力清点（基于实际代码，零臆测）

### 2.1 API路由清单（20个端点）

| # | 路由 | 方法 | 功能 | 实现状态 | 备注 |
|---|------|------|------|----------|------|
| 1 | `/api/ai/generate-image` | POST | 文生图（OpenAI/gpt-image-2） | ✅ 完整实现 | 支持prompt增强、参考图、多尺寸 |
| 2 | `/api/ai/generate-image-ideogram` | POST | 文生图（Ideogram） | ✅ 完整实现 | 备用图生图通道 |
| 3 | `/api/ai/generate-video` | POST | 图生视频（OpenAI兼容中转） | ✅ 完整实现 | SSE流式进度返回 |
| 4 | `/api/ai/generate-video-vidu` | POST | 图生视频（阿里云Vidu） | ✅ 完整实现 | 支持I2V/T2V/首尾帧，8种模型 |
| 5 | `/api/ai/tts` | POST | 文本转语音（Kokoro） | ✅ 完整实现 | SSE流式返回 |
| 6 | `/api/ai/chat/stream` | POST | AI聊天（SSE流式） | ✅ 完整实现 | 支持多模型、图片生成副产物 |
| 7 | `/api/ai/chat` | POST | AI聊天（非流式） | ✅ 完整实现 | 备用接口 |
| 8 | `/api/ai/bible-director` | POST | 角色Bible导演（LLM编排） | ✅ 完整实现 | 六层身份锚点生成 |
| 9 | `/api/ai/generate-character-view` | POST | 角色三视图生成 | ✅ 完整实现 | front/side/back/all |
| 10 | `/api/ai/generate-with-pose` | POST | 姿势参考生图 | ✅ 完整实现 | 支持姿势控制 |
| 11 | `/api/ai/focus-edit` | POST | 焦点编辑（局部重绘） | ✅ 完整实现 | 魔法棒式局部编辑 |
| 12 | `/api/ai/camera-control` | POST | 运镜控制 | ✅ 完整实现 | 镜头语言参数化 |
| 13 | `/api/ai/generate-moodboard` | POST | 情绪板生成 | ✅ 完整实现 | 风格参考聚合 |
| 14 | `/api/ai/generate-poster` | POST | 海报生成 | ✅ 完整实现 | 宣传物料 |
| 15 | `/api/ai/upscale` | POST | 图片放大 | ⚠️ 部分实现 | API框架就绪，可能缺上游 |
| 16 | `/api/ai/talking-photo` | POST | 数字人/对口型 | ⚠️ 部分实现 | API框架就绪 |
| 17 | `/api/ai/reverse-prompt` | POST | 反推提示词 | ✅ 完整实现 | 图生文 |
| 18 | `/api/ai/remix-analysis` | POST | 视频 remix 分析 | ❌ 仅框架 | 前端调用存在但功能待验证 |
| 19 | `/api/ai/config` | GET | AI配置读取 | ✅ 完整实现 | 模型列表、 provider 配置 |
| 20 | `/api/ai/health` | GET/POST | 健康检查 | ✅ 完整实现 | 带 provider 覆盖测试 |

**API层总结**：20个端点中，**14个完整实现，3个部分实现（upscale/talking-photo/remix），3个框架就绪**。核心创作链路（生图、生视频、TTS、聊天）全部真实可用。

---

### 2.2 节点类型清单（19种节点组件）

| # | 节点组件 | 对应kind | 功能 | 实现状态 | 备注 |
|---|----------|----------|------|----------|------|
| 1 | `ContentNode.tsx` | `text`, `prompt`, `script`, `storyboard`, `subtitle` | 文本/脚本/分镜/字幕 | ✅ 完整 | 富文本编辑、Markdown、ContinuityCheckPanel集成 |
| 2 | `ImageNode.tsx` | `image`, `image-result`, `moodboard` | 图片素材/结果 | ✅ 完整 | 上传、预览、AI变体生成 |
| 3 | `VideoNode.tsx` | `video-generation`, `video-result` | 视频生成/结果 | ✅ 完整 | 播放器、字幕叠加、进度显示 |
| 4 | `WorkflowNode.tsx` | `workflow` | 工作流编排 | ✅ 完整 | 运行按钮、状态显示、DAG执行 |
| 5 | `AgentNode.tsx` | `agent` | AI Agent | ✅ 完整 | 多Agent协作入口 |
| 6 | `ShotNode.tsx` | `shot` | 镜头节点 | ✅ 完整 | 分镜详情、时长、运镜 |
| 7 | `SketchNode.tsx` | `sketch` | 草图节点 | ✅ 完整 | 手绘板、草图转图 |
| 8 | `StoryboardGridNode.tsx` | `storyboard-grid` | 分镜网格 | ✅ 完整 | 多镜头网格展示 |
| 9 | `ContinuityReportNode.tsx` | `continuity-report` | 连续性报告 | ✅ 完整 | 六维检查结果展示 |
| 10 | `NodeRunStatusIndicator.tsx` | — | 运行状态指示器 | ✅ 完整 | idle/running/done/error/stale |
| 11 | `CinemaLabPanel.tsx` | — | Cinema Lab控制面板 | ✅ 完整 | 角色三视图、角度控制 |
| 12 | `AngleControlPanel.tsx` | — | 多角度切换面板 | ✅ 完整 | front/side/back视角切换 |
| 13 | `PoseEditor.tsx` | — | 姿势编辑器 | ✅ 完整 | 火柴人姿态参考 |
| 14 | `FocusEditPanel.tsx` | — | 焦点编辑面板 | ✅ 完整 | 魔法棒局部重绘 |
| 15 | `VoicePanel.tsx` | — | 语音/配音面板 | ✅ 完整 | TTS参数控制 |
| 16 | `BgmPanel.tsx` | — | 背景音乐面板 | ✅ 完整 | 音频上传、音量控制 |
| 17 | `TransitionPicker.tsx` | — | 转场选择器 | ✅ 完整 | 视频转场效果 |
| 18 | `BatchProgressBar.tsx` | — | 批量进度条 | ✅ 完整 | 多任务并发进度 |
| 19 | `CharacterBiblePanel.tsx` | — | 角色Bible面板 | ✅ 完整 | 六层身份锚点编辑 |

**节点层总结**：**19种节点全部有真实组件代码，不是占位符**。但部分节点之间的数据流连接逻辑需要进一步完善。

---

### 2.3 工作流执行器真实能力（useWorkflowRunner.ts）

**已实现的执行步骤（WORKFLOW_ORDER）**：

```typescript
const WORKFLOW_ORDER = [
  "text",              // ✅ 文本节点（内容承载）
  "script",            // ✅ 脚本节点（LLM生成剧本）
  "storyboard",        // ✅ 分镜节点（LLM拆分镜头）
  "image-generation",  // ✅ 生图节点（调用/api/ai/generate-image）
  "image-result",      // ✅ 图片结果节点（展示生成结果）
  "video-sample-frames", // ⚠️ 视频抽帧（mock实现）
  "video-analyze",     // ⚠️ 视频分析（mock实现）
  "video-generation",  // ✅ 视频生成节点（调用Vidu/OpenAI视频API）
  "audio",             // ✅ 音频节点（调用/api/ai/tts）
  "subtitle",          // ✅ 字幕节点（LLM生成时间轴字幕）
  "composition",       // ⚠️ 合成节点（仅LLM文本描述，无真实视频合成）
  "video-result",      // ✅ 视频结果节点（展示最终视频）
]
```

**执行能力总结**：
- ✅ **可以真实执行的节点**：text → script → storyboard → image-generation → image-result → video-generation → audio → subtitle → video-result
- ⚠️ **mock/半实现**：video-sample-frames、video-analyze（纯mock数据）、composition（只有文本描述，无真实视频合成引擎）
- ✅ **连续性检查**：ContinuityGuard动态导入，六维检查（时间/空间/角色/道具/情绪/逻辑）真实运行
- ❌ **缺少**：真实视频合成（把多段视频+音频+字幕合成为一个MP4）

---

### 2.4 特色功能真实状态

| 功能 | 状态 | 说明 |
|------|------|------|
| **紫微斗数角色设计** | ❌ 零代码 | 类型定义存在，但无任何实现代码 |
| **ContinuityGuard六维连续性** | ✅ 完整实现 | 399行独立引擎，真实运行 |
| **角色Bible六层身份锚点** | ✅ 完整实现 | 骨骼/面容/体态/服装/气质/ signature |
| **角色三视图生成** | ✅ API就绪，前端UI部分就绪 | /api/ai/generate-character-view可用，CinemaLabPanel有角度切换UI |
| **姿势参考生图** | ✅ 完整实现 | /api/ai/generate-with-pose + PoseEditor |
| **焦点编辑** | ✅ 完整实现 | /api/ai/focus-edit + FocusEditPanel |
| **运镜控制** | ✅ 完整实现 | /api/ai/camera-control |
| **Chat@引用节点** | ❌ 未实现 | ChatPanel能读取节点列表，但AI回复中无action执行 |
| **Chat创建/修改节点** | ❌ 未实现 | 缺少CanvasActionExecutor桥接层 |
| **工作流克隆/模板** | ❌ 未实现 | 无社区/模板系统 |
| **stale状态传播** | ✅ 部分实现 | 有stale定义，但传播逻辑需完善 |

---

## 三、星轨画布 vs TapNow 对标分析

### 3.1 功能对标矩阵

| 维度 | TapNow | 星轨画布 | 差距 |
|------|--------|----------|------|
| **节点式画布** | ✅ 黑色网格+节点 | ✅ React Flow实现 | 持平 |
| **Chat读取画布** | ✅ AI自动读取所有节点 | ⚠️ 传给AI上下文，但AI不能操作 | TapNow胜 |
| **@引用节点** | ✅ 输入框@弹出节点列表 | ❌ 无 | TapNow胜 |
| **AI创建节点** | ✅ "帮我生成分镜节点" | ❌ 无 | TapNow胜 |
| **AI连接节点** | ✅ 自动连线 | ❌ 无 | TapNow胜 |
| **剧本生成** | ✅ 底部AI输入框 | ✅ 类似实现 | 持平 |
| **分镜拆分** | ✅ 一键拆分 | ✅ 工作流执行器支持 | 持平 |
| **生图** | ✅ 多模型选择 | ✅ OpenAI/Ideogram双通道 | 持平 |
| **图生视频** | ✅ 多种模型 | ✅ Vidu 8种模型+OpenAI兼容 | 星轨胜（模型更多） |
| **角色一致性** | ✅ 三视图锁定 | ⚠️ API就绪，前端UI部分就绪 | 差距2天 |
| **姿势控制** | ✅ 火柴人参考 | ✅ PoseEditor实现 | 持平 |
| **焦点编辑** | ✅ 魔法棒 | ✅ FocusEditPanel实现 | 持平 |
| **运镜控制** | ✅ 镜头语言参数化 | ✅ CameraControl实现 | 持平 |
| **Cinema Lab** | ✅ 专业控制面板 | ✅ CinemaLabPanel实现 | 持平 |
| **TTS配音** | ✅ | ✅ Kokoro实现 | 持平 |
| **字幕生成** | ✅ | ✅ LLM生成时间轴字幕 | 持平 |
| **视频合成** | ✅ 多段合成MP4 | ❌ 仅文本描述，无真实合成 | TapNow胜 |
| **一键拉片** | ✅ 上传视频自动拆解 | ❌ 无 | TapNow胜 |
| **工作流克隆** | ✅ TapTV 12万+模板 | ❌ 无社区 | TapNow胜 |
| **紫微斗数角色设计** | ❌ 无 | ❌ 类型定义存在但零代码 | 暂无差距 |
| **ContinuityGuard** | ❌ 无 | ✅ 六维连续性检查 | 星轨独有 |
| **角色Bible** | ❌ 无 | ✅ 六层身份锚点 | 星轨独有 |

### 3.2 核心差距（P0级）

1. **Chat不能操作画布** —— 这是TapNow的核心体验。用户说"帮我生成分镜"，TapNow真的创建节点。星轨画布的Chat只能"看到"不能"动手"。
2. **缺少视频合成引擎** —— 星轨能生成多段视频片段，但没有把它们+音频+字幕合成为一个MP4的能力。
3. **没有社区/模板生态** —— TapNow的TapTV有12万+可克隆工作流，这是巨大的用户粘性来源。
4. **角色三视图前端UI** —— API已就绪，但缺少独立参数面板。

### 3.3 星轨独有优势

1. **ContinuityGuard六维连续性** —— TapNow完全没有。这是影视制作的核心质量保障。
2. **角色Bible六层身份锚点** —— 从骨骼结构到signature scent的深度角色管理。
3. **Vidu 8种模型支持** —— 比TapNow的视频模型选择更丰富。
4. **开源+本地化** —— 全代码开源，无平台锁定。

---

## 四、Bug清单（基于代码扫描）

### P0 阻塞级（1项）—— 必须立即修复

| # | 文件 | 位置 | 问题 | 修复 |
|---|------|------|------|------|
| 1 | `StarCanvas.tsx` | L625 | `setInterval` 缺少 `clearInterval` cleanup，组件卸载后定时器持续运行，内存泄漏 | useEffect返回cleanup函数 |

### P1 重要级（16项）—— 本周内修复

| # | 文件 | 位置 | 问题 | 修复 |
|---|------|------|------|------|
| 2 | `VideoNode.tsx` | L114 | `removeEventListener` 使用箭头函数，与add时引用不同，永远无法移除监听器 | 使用具名函数 |
| 3 | `CharacterBiblePanel.tsx` | L238 | `group-hover:opacity-100` 但父级无`group`类，删除按钮不显示 | 父级div添加`group`类 |
| 4 | `CharacterBiblePanel.tsx` | L115 | fetch后未检查`res.ok`，错误响应会被当正常数据解析 | 添加`if (!res.ok)`检查 |
| 5 | `CharacterBiblePanel.tsx` | 多处 | 大量使用`prev.identityAnchors!`非空断言，undefined时崩溃 | 使用可选链`?.`配合默认值 |
| 6 | `useWorkflowRunner.ts` | L209+ | `any`类型泛滥（parsed: any, n: any, nds: any[]） | 定义具体接口 |
| 7 | `StarCanvas.tsx` | L570 | `useCanvasStore()`全量解构，任何字段变化触发整个StarCanvas重渲染 | 使用selector细粒度订阅 |
| 8 | `tts/route.ts` | L64+ | 整个POST handler缺少顶层try-catch | 包裹try-catch |
| 9 | `bible-director/route.ts` | L18 | `body`直接`as`断言，无输入验证 | 添加Zod或手动验证 |
| 10 | `continuityGuard.ts` | L345 | `collectShotText`缺少空值保护，`.map()`可能崩溃 | 使用`(shot.instructions?.fragments ?? [])` |
| 11 | `ContentNode.tsx` | L173 | `issues.map((issue, idx) => <div key={idx}>)`使用索引作为key | 使用`issue.shotId`或`issue.sceneId` |
| 12 | `CharacterBiblePanel.tsx` | L47 | useEffect依赖`bibleCharacters`数组，store更新导致频繁setEdit | 仅订阅selectedBibleCharacterId |
| 13 | `generate-image/route.ts` | 多处 | >30处console.info/debug，生产环境泄露内部状态 | 使用logger封装，按NODE_ENV控制 |
| 14 | `chat/stream/route.ts` | L713 | console.error可能泄露上游API敏感信息 | 脱敏后记录 |
| 15 | `useWorkflowRunner.ts` | L506 | 异步操作中setNodes没有检查组件是否已卸载 | 维护isMounted ref |
| 16 | `StarCanvas.tsx` | L512+ | 频繁使用类型断言`as`，可通过类型守卫避免 | 添加isCanvasNodeData守卫函数 |
| 17 | 全项目 | >30处 | `catch (error: any)`泛滥，应收敛为`unknown` | 统一使用`catch (error: unknown)` |

### P2 建议级（18项）—— 渐进优化

- `CharacterBiblePanel.tsx` L76：使用原生`alert()`不符合现代UI实践
- `ContentNode.tsx` L949：img标签缺少onError，图片加载失败无处理
- `AddNodePanel.tsx` L283：`continuity-report`节点未验证workflow处理逻辑
- `useWorkflowRunner.ts` L1138：`process.env.NEXT_PUBLIC_VIDEO_BACKEND as any`
- `StarCanvas.tsx` L7327：`// TODO: 接入真实登录`
- `videoGenerationService.ts` L197：`// TODO: Replace with real API call`
- `continuityGuard.ts` L304：未使用变量`propPattern`
- `continuityGuard.ts` L243：时间映射不完整，中文变体覆盖不足
- `ChatPanel.tsx` L280：useCallback空依赖闭包陷阱
- `ContentNode.tsx` L304：useEffect依赖data对象字段但传入整个data
- `StarCanvas.tsx` L4064：settings event listeners依赖不稳定函数引用
- `useWorkflowRunner.ts` L1287：executeStep依赖数组包含updateNodeData

---

## 五、我们应该如何向用户展示真实能力？

### 5.1 当前可以真实演示的完整流程（MVP链路）

**建议演示路径**：
```
1. 打开星轨画布 → 黑色网格画布
2. 上传角色三视图 → ImageNode出现
3. 右侧Chat输入："分析这张角色图"
4. 新建Text节点 → 写下视频目标
5. 底部AI输入框："基于这个角色，生成30秒科幻短片脚本"
6. AI生成ScriptNode
7. 运行工作流 → 自动拆分6个分镜节点
8. 每个分镜连接角色图 → 生图节点 → 生成分镜图
9. 分镜图连接视频节点 → 调用Vidu生成视频片段
10. 添加旁白Text节点 → 连接语音节点生成音频
11. 展示最终多段视频+音频结果
```

**注意事项**：
- 视频合成（多段合为一个MP4）需要说明"正在开发中"
- Chat不能自动创建节点，需要手动操作
- 角色三视图需要在CinemaLabPanel中手动切换角度

### 5.2 建议的展示策略

1. **主打"专业控制"差异化** —— 强调CinemaLab、姿势编辑、焦点编辑、运镜控制，这些是TapNow也没有的深度控制。
2. **主打"玄学+编剧"融合** —— 紫微斗数角色设计（需先实现）、ContinuityGuard六维连续性检查、角色Bible六层身份锚点。
3. **诚实展示边界** —— 明确告知用户哪些已就绪、哪些开发中。不要过度承诺。
4. **开源透明** —— 展示GitHub仓库、API路由代码、节点组件代码，让用户看到是真实实现而非demo。

### 5.3 优先修复清单（展示前必须完成）

** blocker级别（不修复无法演示）**：
- [ ] P0: StarCanvas.tsx setInterval内存泄漏
- [ ] P1: VideoNode事件监听器无法移除（可能导致视频播放异常）
- [ ] P1: CharacterBiblePanel删除按钮不显示
- [ ] P1: CharacterBiblePanel fetch错误处理

** 体验级别（修复后更流畅）**：
- [ ] P1: useWorkflowRunner any类型替换（避免执行时类型错误）
- [ ] P1: continuityGuard空值保护（避免检查时报错）
- [ ] P1: 清理生产环境console语句
- [ ] P2: ContentNode img onError处理

---

## 六、下一步行动建议

### 立即行动（今天）
1. 修复P0 setInterval内存泄漏
2. 修复P1 VideoNode事件监听器问题
3. 修复P1 CharacterBiblePanel的group-hover和fetch错误

### 短期（本周）
4. 修复P1 any类型泛滥问题
5. 修复P1 API路由try-catch和输入验证
6. 清理生产环境console语句
7. 实现Chat操作画布的桥接层（CanvasActionExecutor）

### 中期（本月）
8. 实现真实视频合成引擎（FFmpeg.wasm或后端合成）
9. 完成角色三视图前端UI面板
10. 实现紫微斗数角色设计模块（从类型定义到完整实现）
11. 添加工作流模板系统

### 长期
12. 构建社区/模板生态
13. 一键拉片功能
14. 剪映草稿导出

---

**报告生成时间**：2026-06-10 12:00
**数据来源**：实际代码扫描（零臆测）
**检查范围**：apps/web/src 全部TypeScript/React代码 + 20个API路由
